<h1>Doses#new</h1>
<p>Find me in app/views/doses/new.html.erb</p>


<div class="container">
  <h2>Add a dose for the <%= @cocktail.name %></h2>
  <div class="row">
    <div class="col-6 offset-3">
  <%= simple_form_for([@cocktail,@dose]) do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all %>
  <%= f.submit %>
    </div>
  <% end %>
  </div>
</div>
